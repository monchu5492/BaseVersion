// const nodemailer = require('nodemailer');

const sendinvite = (user, link) => {
  console.log(`Dev invite send to ${user} for ${link}`);
  // const emailParts = user.split();
};

export default sendinvite;
